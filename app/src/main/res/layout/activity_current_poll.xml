<?xml version="1.0" encoding="utf-8"?>

<layout>
    <data>
        <variable
            name = "poll"
            type = "mattoncino.pollo.Poll" />
    </data>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/activity_current_poll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="mattoncino.pollo.CurrentPollActivity">

        <TextView
            android:text="@{poll.name}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/nameTextView"
            android:textSize="20dp"
            android:paddingBottom="10dp" />

        <TextView
            android:text="@{poll.question}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/nameTextView"
            android:id="@+id/questionTextView"
            android:paddingBottom="10dp" />

        <TextView
            android:text="@{poll.firstOpt}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/questionTextView"
            android:id="@+id/opt1TextView"
            android:paddingBottom="10dp" />

        <TextView
            android:text="@{poll.secondOpt}"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/opt1TextView"
            android:id="@+id/opt2TextView"
            android:paddingBottom="10dp" />
    </RelativeLayout>
</layout>