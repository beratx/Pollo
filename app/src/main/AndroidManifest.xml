<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mattoncino.pollo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreatePollActivity" />
        <activity android:name=".MultiOptPollActivity" />
        <activity android:name=".ActivePollsActivity" />
        <activity android:name=".OldPollsActivity" />
        <activity android:name=".PollDetailActivity" />
        <activity android:name=".CurrentPollActivity"></activity>
    </application>

</manifest>